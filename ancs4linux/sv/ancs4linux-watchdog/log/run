#!/bin/sh
# Without this the watchdog's output goes to runit's console and is lost, which
# makes "when did the advert die, and did it come back?" unanswerable later.
exec svlogd -tt /var/log/ancs4linux-watchdog
